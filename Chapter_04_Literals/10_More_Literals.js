let count = 42; // This is a numeric literal assigned to the variable 'count'.
let negative = -100; // This is a negative numeric literal assigned to the variable 'negative'.
let zero = 0; // This is a numeric literal assigned to the variable 'zero'.

let h = 0xFF; // This is a hexadecimal numeric literal assigned to the variable 'h'.
console.log(typeof h); // "number"
let color_hex = 0xFF0000; // This is a hexadecimal numeric literal representing the color red in RGB format.

let octal = 0o77; // This is an octal numeric literal assigned to the variable 'octal'.
let million = 1e6; // This is a numeric literal representing one million.
let tiny = 1.5e-4; // This is a numeric literal representing a small decimal value.